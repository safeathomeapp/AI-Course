# T1M02 Revision Plan (Evidence-Based)

## High-priority changes
1. Re-sequence early concept flow to reduce non-technical drop-off risk.
   - Bring practical “confident error / why this matters” framing earlier.
   - Defer heavier mechanics detail until relevance is established.
2. Add plain-language term support at first use.
   - Prioritize `tokens`, `tokenization`, `context window` with concise definitions and examples.
3. Strengthen guided-exercise calibration.
   - Add one full worked example (Prompt A vs Prompt B, outputs, scoring rationale).
   - Add explicit quality criteria for uncertainty-aware prompts.
4. Expand demonstration transfer coverage with minimal scope increase.
   - Keep software + finance, but add one non-technical/frontline/admin-style variant or mapped callout.

## Medium-priority changes
1. Tighten tokenization depth for practitioner baseline.
   - Preserve accuracy but reduce surplus mechanism detail for general track.
2. Improve artefact onboarding guidance.
   - Include a completed Control Card sample.
   - Clarify how to fill fields consistently for typical work tasks.
3. Add learner expectation guidance for timing variability.
   - Keep baseline estimate, add note that some profiles may require longer.
4. Add optional regulated-context callout.
   - Briefly connect hallucination/uncertainty controls to compliance-sensitive workflows.

## Low-priority changes
1. Add optional advanced deep-dive path for high-fluency learners.
2. Add optional role-specific example appendix for enterprise customization.
3. Add optional team-implementation prompts for manager/L&D cohorts.

## Rejected suggestions
1. Remove technical mechanics entirely from T1M02.
   - Rejected: contradicts module objective (how LLMs work).
2. Rebuild the full module into separate role-specific versions inside core content.
   - Rejected: high complexity and scope expansion for a foundation unit.
3. Add extensive procurement/ROI/LMS operational content into module body.
   - Rejected: belongs in deployment documentation, not core learning narrative.
4. Expand into full architecture-level deep dive (sampling parameter theory, model internals) in core track.
   - Rejected: power-user request, not representative foundation need.

## Notes on what should not be changed in the module
1. Do not remove the pipeline mental model with explicit human verification.
2. Do not remove the probability-ranking/hallucination explanation core.
3. Do not remove the comparative guided exercise format.
4. Do not remove the Reliability Control Card artefact concept.
5. Do not weaken accountability framing (verification before decision use).

